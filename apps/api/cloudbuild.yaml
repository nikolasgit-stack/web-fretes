steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/api/Dockerfile
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

substitutions:
  _IMAGE: southamerica-east1-docker.pkg.dev/PROJECT_ID/web-fretes/web-fretes-api:latest
